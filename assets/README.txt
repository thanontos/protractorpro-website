Drop app screenshots here (PNG). The site auto-hides missing ones:

  shot-digitizer.png   - Digitizer with a plan loaded + measurements drawn
  shot-conditions.png  - Condition editor (layers/fasteners) or conditions panel
  shot-recap.png       - Job recap / markup tab with totals

Tip: capture with Win+Shift+S at a wide window size (~1600px+),
avoid real customer names/prices, then redeploy.
